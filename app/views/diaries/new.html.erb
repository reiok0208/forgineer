<div class="row">
  <div class="col-xs-8 col-xs-offset-2">
    <h1>新規投稿</h1>
    <div style="padding-left:30px;">
      <strong style="font-size:20px;">関連タグ</strong>
      <p style="font-size:15px;">⚠新規追加したいタグがある場合は下記フォームに記入して追加ボタンを押してください</p>
      <p style="font-size:15px;">一括で追加したい場合は<strong>,（カンマ）</strong>で区切って追加ボタンを押してください</p>
      <%= form_tag(diaries_new_user_diaries_path, method: :post) do %>
        <%= text_field_tag :name,"",class: "form-control",placeholder: "タグは全てスペースなし、小文字で保存されます",style: "width:50%; margin:5px; float:left;" %>
        <%= submit_tag "タグを追加", class: "btn btn-default",style: "margin:5px;" %>
      <% end %>
      <strong style="font-size:20px;">関連タグ選択</strong>
    </div>

    <div style="padding-left:20px;">
      <%= form_for @diary, url: user_diaries_path, data: { persist: 'garlic' } do |f| %>
        <%= render "layouts/post", f: f %>
        <%= f.submit "新規投稿", class: "btn btn-default btn-lg", style: "float:right;" %>
      <% end %>
    </div>
  </div>
</div>